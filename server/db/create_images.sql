INSERT INTO mountains (title, description, imageurl)
VALUES ($1, $2, $3);



-- CREATE TABLE mountains
-- (
--     id serial PRIMARY key,
--     title varchar(50),
--     description VARCHAR(255),
--     imageurl text
-- );
